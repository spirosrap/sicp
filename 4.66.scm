;;; Query input:
(wheel ?who)
;;; Query results:
(wheel (warbucks oliver))
(wheel (warbucks oliver))
(wheel (bitdiddle ben))
(wheel (warbucks oliver))

;if for example someone would try to find the sum of all the wheels salaries it would count warbucks oliver's 3 times.
;We should find a way to remove the duplicates.